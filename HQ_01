// HOMEWORK QUESTIONS 01

#include <stdio.h>
#include <math.h>

int main()
{
    float x,y;
    printf("Enter the value of x: ");
    scanf("%f",&x);

    y=((5*pow(x,3))-(4*x*x)+sqrt(x)+3);

    printf("The result of the expression is: %.2f",y);


return 0;
}
